# PostgreSQL connection string (Railway lo inyecta automáticamente)
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Puerto (Railway lo asigna automáticamente)
PORT=3000

# Contraseña del administrador (cambia esto en Railway → Variables)
ADMIN_PASSWORD=admin1234
